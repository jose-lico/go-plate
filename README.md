# go-plate

Opinionated Go Backend Example

## make commands

`make dev` -> live reload thru [air](https://github.com/air-verse/air)

`make run` -> go run

`make build` -> go build

---

- Uses chi router
